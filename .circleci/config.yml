version: 2.1

jobs:
  build:
    docker:
      - image: node:17
    steps:
      - run: npm run lint
  test:
    docker:
      - image: node:17
    steps:
      - run: npm run test
  analyze:
    docker:
      - image: node:17
    steps:
      - run: npm run audit

workflows:
  node-workflow:
    jobs:
      - build
      - test:
          requires:
            - "build"
      - analyze:
          requires:
            - "test"
